// Placeholder auth hook
// In future iterations this hook will manage authentication state,
// tokens, and user profile for the meeting application.

const useAuth = () => {
  return {
    user: null,
    isAuthenticated: false,
  };
};

export default useAuth;


